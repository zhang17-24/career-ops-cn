# career-ops-plugin-{{NAME}}

Deterministic, zero-token recruitment provider for {{COMPANY}} (`{{HOST}}`).

The generated parser is a starting point. Inspect the public endpoint with Ego Lite, update `index.mjs`, and add a saved fixture before enabling it.
